# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.md

# Saída local do cargo-tarpaulin (o CI usa o mesmo caminho)
coverage/

# Credenciais OAuth (use .env.example como modelo)
.env

# Configuração do Cloudflare Worker
worker/wrangler.toml

# Artefatos de build
/x86_64

src-tauri/gen/apple/
src-tauri/gen/android/**/key.properties
src-tauri/gen/android/**/keystore.properties

# Keystores de assinatura Android — nunca commitar
*.jks
*.keystore

# Chave privada de assinatura do updater — mora fora do repositório, mas a
# regra evita o acidente de gerá-la aqui dentro. A .pub é pública e fica no
# tauri.conf.json.
*.key
!*.key.pub

# Ferramentas locais
.agents/
skills-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/
CLAUDE.md